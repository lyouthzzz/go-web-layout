syntax = "proto3";

package api;
import "errors/errors.proto";

option go_package = "github.com/lyouthzzz/go-web-layout/api;api";

enum ErrorReason {
  option (errors.default_code) = 500;

  BAD_REQUEST = 0 [(errors.code) = 400];
  NOT_FOUNT = 1 [(errors.code) = 404];
}